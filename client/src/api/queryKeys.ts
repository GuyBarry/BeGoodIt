export const queryKeys = {
  colorGroups: {
    getAll: ['colorGroups'],
  },
  garmentCategories: {
    getAll: ['garmentCategories'],
  },
  genders: {
    getAll: ['genders'],
  },
  seasons: {
    getAll: ['seasons'],
  },
} as const;
